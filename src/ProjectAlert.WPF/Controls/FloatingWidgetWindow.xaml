<Window x:Class="ProjectAlert.WPF.Controls.FloatingWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="320" Height="200"
        MinWidth="200" MinHeight="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="{Binding IsTopmost, Mode=OneWay}"
        Opacity="{Binding WindowOpacity, Mode=OneWay}"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border x:Name="OuterBorder" Background="#37474F" CornerRadius="12" BorderBrush="#455A64" BorderThickness="1" SizeChanged="OuterBorder_SizeChanged">
        <!-- 移除 DropShadowEffect 以减少渲染资源占用 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 内容区（占据整个空间） -->
            <ContentPresenter x:Name="ContentArea" Grid.Row="0"
                              Content="{Binding WidgetContent, RelativeSource={RelativeSource AncestorType=Window}}"
                              Margin="8"/>

            <!-- 状态栏 -->
            <Border Grid.Row="1" Background="#263238" CornerRadius="0,0,12,12" Padding="8,4"
                    Visibility="{Binding ShowStatusBar, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource Boolean2VisibilityConverter}}">
                <TextBlock Text="{Binding LastUpdateTime, StringFormat='更新于: {0:HH:mm:ss}'}"
                           Foreground="#78909C" FontSize="10" VerticalAlignment="Center"/>
            </Border>

            <!-- 右下角调整大小手柄 -->
            <ResizeGrip Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                        Foreground="#546E7A" Margin="0,0,4,4"
                        Visibility="{Binding CanResize, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource Boolean2VisibilityConverter}}"/>

            <!-- 浮动标题栏（覆盖在内容上方） -->
            <Border x:Name="TitleBar" Grid.Row="0" Grid.RowSpan="2"
                    VerticalAlignment="Top" Height="36"
                    Background="#E6263238" CornerRadius="12,12,0,0">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="TitleBarTransform" Y="0"/>
                </Border.RenderTransform>
                <Grid>
                    <TextBlock Text="{Binding Title}" Foreground="White" VerticalAlignment="Center"
                               Margin="14,0,0,0" FontWeight="SemiBold" FontSize="13"
                               TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
                        <!-- 透明度按钮（悬停显示滑块） -->
                        <Grid x:Name="OpacityButtonContainer">
                            <Button x:Name="BtnOpacity" Width="28" Height="28" Background="Transparent"
                                    Foreground="#B0BEC5" BorderThickness="0" Cursor="Hand" FontSize="12"
                                    ToolTip="调节透明度">
                                <TextBlock Text="◐" FontSize="14"/>
                            </Button>
                            <Popup x:Name="OpacityPopup" PlacementTarget="{Binding ElementName=BtnOpacity}"
                                   Placement="Bottom" AllowsTransparency="True" StaysOpen="True"
                                   PopupAnimation="Fade">
                                <Border x:Name="OpacityPopupBorder" Background="#1E1E1E" CornerRadius="6" Padding="10,8"
                                        BorderBrush="#455A64" BorderThickness="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="透明度" Foreground="#B0BEC5" FontSize="11"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <Slider x:Name="OpacitySlider" Width="80" Minimum="0.2" Maximum="1.0"
                                                Value="{Binding WindowOpacity, Mode=TwoWay}"
                                                VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </Grid>
                        <Button x:Name="BtnLock" Width="28" Height="28" Background="Transparent"
                                Foreground="{Binding IsLocked, Converter={StaticResource BoolToColorConverter}}"
                                BorderThickness="0" Click="LockButton_Click" Cursor="Hand" FontSize="12"
                                ToolTip="锁定/解锁位置和大小">
                            <TextBlock Text="{Binding IsLocked, Converter={StaticResource BoolToLockIconConverter}}" FontSize="14" FontWeight="Bold"/>
                        </Button>
                        <Button x:Name="BtnTopmost" Width="28" Height="28" Background="Transparent"
                                Foreground="{Binding IsTopmost, Converter={StaticResource BoolToColorConverter}}"
                                BorderThickness="0" Click="TopmostButton_Click" Cursor="Hand" FontSize="12"
                                ToolTip="置顶/取消置顶">
                            <TextBlock Text="📌" FontSize="12"/>
                        </Button>
                        <Button Content="⟳" Width="28" Height="28" Background="Transparent" Foreground="#B0BEC5"
                                BorderThickness="0" Click="RefreshButton_Click" Cursor="Hand" FontSize="14"
                                ToolTip="刷新"/>
                        <Button Content="—" Width="28" Height="28" Background="Transparent" Foreground="#B0BEC5"
                                BorderThickness="0" Click="MinimizeButton_Click" Cursor="Hand" FontSize="14"/>
                        <Button Content="×" Width="28" Height="28" Background="Transparent" Foreground="#B0BEC5"
                                BorderThickness="0" Click="CloseButton_Click" Cursor="Hand" FontSize="14"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
