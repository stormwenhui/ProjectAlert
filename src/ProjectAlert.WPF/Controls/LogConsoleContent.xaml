<UserControl x:Class="ProjectAlert.WPF.Controls.LogConsoleContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ProjectAlert.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="450"
             d:DataContext="{d:DesignInstance Type=vm:LogConsoleViewModel}">
    <UserControl.Resources>
        <!-- 日志级别颜色转换 -->
        <Style x:Key="LogLevelStyle" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="Debug">
                    <Setter Property="Foreground" Value="#78909C"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Info">
                    <Setter Property="Foreground" Value="#4FC3F7"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Warning">
                    <Setter Property="Foreground" Value="#FFB74D"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Error">
                    <Setter Property="Foreground" Value="#EF5350"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="#263238" Padding="6,4" Margin="0,30,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 搜索框 -->
                <Border Background="#37474F" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <Grid>
                        <TextBlock Text="搜索..." Foreground="#78909C" FontSize="11"
                                   VerticalAlignment="Center" Margin="2,0,0,0"
                                   Visibility="{Binding SearchKeyword, Converter={StaticResource String2InverseVisibilityConverter}}"/>
                        <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="White" FontSize="11"
                                 VerticalContentAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- 筛选按钮组 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ItemsControl ItemsSource="{Binding LevelFilters}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ToggleButton IsChecked="{Binding IsChecked}" Content="{Binding Name}"
                                              Margin="2,0" Padding="6,2" FontSize="10"
                                              Foreground="White" BorderThickness="0" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="border" Background="#455A64" CornerRadius="3" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#546E7A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#607D8B"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- 自动滚动 -->
                    <ToggleButton IsChecked="{Binding AutoScroll}" ToolTip="自动滚动"
                                  Margin="6,0,0,0" Padding="4,2" FontSize="10"
                                  Foreground="White" BorderThickness="0" Cursor="Hand">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="border" Background="#455A64" CornerRadius="3" Padding="{TemplateBinding Padding}">
                                                <TextBlock x:Name="icon" Text="⬇" FontSize="12"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#4CAF50"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>

                    <!-- 清空按钮 -->
                    <Button Command="{Binding ClearLogsCommand}" ToolTip="清空日志"
                            Margin="4,0,0,0" Padding="4,2" FontSize="10"
                            Background="Transparent" Foreground="#B0BEC5"
                            BorderThickness="0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="#455A64" CornerRadius="3" Padding="{TemplateBinding Padding}">
                                                <TextBlock Text="&#x1F5D1;" FontSize="11"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#E53935"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 日志列表 -->
        <ListBox x:Name="LogListBox" Grid.Row="1"
                 ItemsSource="{Binding LogsView}"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="6,2"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}"
                                        BorderBrush="#37474F" BorderThickness="0,0,0,1">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#3E5058"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#455A64"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="复制"
                                          Command="{Binding DataContext.CopyLogCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                          CommandParameter="{Binding}"/>
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 时间 -->
                        <TextBlock Grid.Column="0" Text="{Binding TimeText}"
                                   Foreground="#78909C" FontSize="11" FontFamily="Consolas"
                                   VerticalAlignment="Top"/>

                        <!-- 来源 -->
                        <Border Grid.Column="1" Background="#455A64" CornerRadius="2"
                                Padding="4,1" Margin="4,0">
                            <TextBlock Text="{Binding Source}" Foreground="#B0BEC5"
                                       FontSize="10" VerticalAlignment="Center"/>
                        </Border>

                        <!-- 消息 -->
                        <TextBlock Grid.Column="2" Text="{Binding Message}"
                                   Style="{StaticResource LogLevelStyle}"
                                   FontSize="11" TextWrapping="Wrap"
                                   VerticalAlignment="Top" Margin="4,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#1E1E1E" Padding="6,3">
            <TextBlock Foreground="#78909C" FontSize="10">
                <Run Text="共"/>
                <Run Text="{Binding LogCount, Mode=OneWay}"/>
                <Run Text="条，显示"/>
                <Run Text="{Binding FilteredLogCount, Mode=OneWay}"/>
                <Run Text="条"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
